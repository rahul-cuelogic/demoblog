<form action="{{ path('app_blog_comment_create', { 'blogId' : comment.postid } ) }}" method="post" {{ form_enctype(form) }} class="blogger">
    {{ form_widget(form) }}
    <p>
        <input type="submit" value="Submit">
    </p>
</form>